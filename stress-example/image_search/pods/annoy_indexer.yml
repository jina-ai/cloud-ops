!CompoundIndexer
components:
  - !AnnoyIndexer
    with:
      #index_filename: 'vec.gz' # Should be overwriten by ref_indexer index filename
      n_trees: 10
      search_k: -1
      ref_indexer:
        !NumpyIndexer
        metas:
          workspace: '/docker_workspace/workspace'
          name: vec_idx
        with:
          index_filename: 'vec.gz'
    metas:
      workspace: '/docker_workspace/workspace'
      name: annoyidx
  - !BinaryPbIndexer
    with:
      index_filename: doc.gz
    metas:
      name: docidx
      workspace: '/docker_workspace/workspace'
metas:
  name: vec_idx
  py_modules:
    - docker_workspace/__init__.py
  workspace: '/docker_workspace/workspace'
requests:
  on:
    SearchRequest:
      - !VectorSearchDriver
        with:
          executor: annoyidx
      - !KVSearchDriver
        with:
          executor: docidx
          traversal_paths: ['m']
    ControlRequest:
      - !ControlReqDriver {}
